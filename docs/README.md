This README exists to facilitate the creation of the docs folder in Github.

Be sure to grant write permissions to Tomcat to this folder so that Orbeon can write uploads into it:

`sudo chown -R tomcat:tomcat docs`
